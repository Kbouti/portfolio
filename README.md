A template repository to base other projects off. 

Webpack is installed, set to automatically build when: npm run watch is active (cntrl + C) to stop

Testing is active, run: npm test to see test results